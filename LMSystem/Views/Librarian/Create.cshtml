@model LibraryManagement.Models.LibrarianModel
@{
    ViewData["Title"] = "Add Librarian";
}
<h2>Add Librarian</h2>
<form asp-action="Create" method="post" onsubmit="return validateForm();">
    <div class="mb-3">
        <label>Name</label>
        <input asp-for="Name" class="form-control" id="LibrarianName" />
    </div>
    <div class="mb-3">
        <label>Age</label>
        <input asp-for="Age" class="form-control" id="LibrarianAge" />
    </div>
    <div class="mb-3">
        <label>Phone</label>
        <input asp-for="Phone" class="form-control" id="LibrarianPhone" />
    </div>
    <button type="submit" class="btn btn-success">Add</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>

<script>
    function validateForm() {
        const name = document.getElementById('LibrarianName').value.trim();
        const age = document.getElementById('LibrarianAge').value.trim();
        const phone = document.getElementById('LibrarianPhone').value.trim();
        if (!name || !age || !phone) {
            alert("All fields are required: Name, Age, Phone.");
            return false; // prevent form submission
        }
        return true; // allow submission
    }
</script>
